import * as types from './actionTypes';


// const distance = (a,b) => {
//     return {
//       type: types.DISTANCE,
//       distance: a ,
//       totalfueluse: b
//     };
//   };

//   getDistance = (speed) => (
//     dispatch => {
//         var a = [] ;
//         var b = [] ;
//             speed.map(function(data , item){
//             a.push(data.distance.toFixed(1));
//             b.push(data.totalfueluse);
//   }.bind(this));
//         dispatch(distance(a,b))
//         }
//     )
